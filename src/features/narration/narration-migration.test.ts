import {readFileSync} from 'node:fs'; import {join} from 'node:path'; import {describe, expect, it} from 'vitest';
describe('narration migration', () => { it('moves only untouched pre-narration OpenAI defaults to Deepgram', () => { const sql = readFileSync(join(process.cwd(), 'drizzle/0009_narration_tracks.sql'), 'utf8'); expect(sql).toContain("SET DEFAULT 'deepgram'"); for (const guard of ['"status" = \'draft\'','"revision" = 0','"current_audit_id" IS NULL','"narration_approved_at" IS NULL','"narration_approval_hash" IS NULL','"audio_approved_at" IS NULL','"narration_track_selection" IS NULL','"creator_narration_asset_id" IS NULL','"narrator_voice" IS NULL','"render_manifest" IS NULL','FROM "narration_samples"','FROM "narration_tracks"']) expect(sql).toContain(guard); }); });
