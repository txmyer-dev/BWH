import {cookies} from 'next/headers'; import {NextResponse} from 'next/server'; import {z} from 'zod';
import {narrationAuditContract, PostgresNarrationRepository} from '@/features/narration/narration-repository'; import {NarrationService} from '@/features/narration/narration-service'; import {safeNarrationRouteError} from '@/features/narration/route-errors'; import {GcsMediaStorage} from '@/features/media/gcs-storage'; import {PostgresProjectRepository} from '@/features/projects/project-repository'; import {ProjectService} from '@/features/projects/project-service'; import {getDatabase} from '@/server/db/client'; import {parseEnv} from '@/server/env'; import {createProviderServices} from '@/server/providers/factory';
const schema = z.discriminatedUnion('kind', [z.object({kind: z.literal('generated'), provider: z.enum(['deepgram','azure']), sampleId: z.string().uuid()}).strict(), z.object({kind: z.literal('creator'), assetId: z.string().uuid()}).strict()]); type Context = {params: Promise<{projectId: string}>};
export async function POST(request: Request, context: Context) { const {projectId} = await context.params; try { const database = getDatabase(); const env = parseEnv(process.env); const token = (await cookies()).get(`legacy_owner_${projectId}`)?.value ?? ''; const projects = new ProjectService(new PostgresProjectRepository(database)); await projects.assertProjectOwner(projectId, token); const body = schema.parse(await request.json()); const services = createProviderServices(env, {database}); const service = new NarrationService(new PostgresNarrationRepository(database, narrationAuditContract(env.OPENAI_AUDIT_MODEL)), services.executor, services.narrationProviders, new GcsMediaStorage(env.GCS_BUCKET), (id) => projects.assertProjectOwner(id, token), env.PROVIDER_FINGERPRINT_SECRET ?? 'development-only-provider-fingerprint', services.azureTtsPriceMicrosPerMillionCharacters); return NextResponse.json(await service.approveAudioSelection(projectId, body)); } catch (error) { const safe = safeNarrationRouteError(error); return NextResponse.json({error: safe.code}, {status: safe.status}); } }
