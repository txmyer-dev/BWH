import {cookies} from 'next/headers'; import {NextResponse} from 'next/server'; import {z} from 'zod';
import {PostgresNarrationRepository} from '@/features/narration/narration-repository'; import {NarrationService} from '@/features/narration/narration-service'; import {safeNarrationRouteError} from '@/features/narration/route-errors'; import {GcsMediaStorage} from '@/features/media/gcs-storage'; import {PostgresProjectRepository} from '@/features/projects/project-repository'; import {ProjectService} from '@/features/projects/project-service'; import {getDatabase} from '@/server/db/client'; import {parseEnv} from '@/server/env'; import {createProviderServices} from '@/server/providers/factory'; import {factualityAuditDeployment} from '@/features/audit/audit-deployment';
const schema = z.object({provider: z.enum(['deepgram','azure'])}).strict(); type Context = {params: Promise<{projectId: string}>};
export async function POST(request: Request, context: Context) { const {projectId} = await context.params; try { const database = getDatabase(); const env = parseEnv(process.env); const token = (await cookies()).get(`legacy_owner_${projectId}`)?.value ?? ''; const projects = new ProjectService(new PostgresProjectRepository(database)); await projects.assertProjectOwner(projectId, token); const body = schema.parse(await request.json()); const services = createProviderServices(env, {database}); const service = new NarrationService(new PostgresNarrationRepository(database, factualityAuditDeployment(env).contract), services.executor, services.narrationProviders, new GcsMediaStorage(env.GCS_BUCKET), (id) => projects.assertProjectOwner(id, token), env.PROVIDER_FINGERPRINT_SECRET ?? 'development-only-provider-fingerprint', services.azureTtsPriceMicrosPerMillionCharacters); const sample = await service.sample(projectId, body.provider); return NextResponse.json({id: sample.id, provider: sample.provider, model: sample.model, voice: sample.voice, durationMs: sample.durationMs, approved: Boolean(sample.approvedAt)}); } catch (error) { const safe = safeNarrationRouteError(error); return NextResponse.json({error: safe.code}, {status: safe.status}); } }
const approvalSchema = z.object({sampleId: z.string().uuid()}).strict();
export async function GET(request: Request, context: Context) { const {projectId} = await context.params; try { const database = getDatabase(); const env = parseEnv(process.env); const token = (await cookies()).get(`legacy_owner_${projectId}`)?.value ?? ''; await new ProjectService(new PostgresProjectRepository(database)).assertProjectOwner(projectId, token); const sampleId = z.string().uuid().parse(new URL(request.url).searchParams.get('sampleId')); const sample = await new PostgresNarrationRepository(database).findSample(projectId, sampleId); if (!sample) throw new Error('NARRATION_SAMPLE_NOT_FOUND'); return NextResponse.json({url: await new GcsMediaStorage(env.GCS_BUCKET).createDownloadUrl({objectKey: sample.objectKey, expiresInMs: 300_000})}); } catch (error) { const safe = safeNarrationRouteError(error); return NextResponse.json({error: safe.code}, {status: safe.status}); } }
export async function PATCH(request: Request, context: Context) { const {projectId} = await context.params; try { const database = getDatabase(); const env = parseEnv(process.env); const token = (await cookies()).get(`legacy_owner_${projectId}`)?.value ?? ''; const projects = new ProjectService(new PostgresProjectRepository(database)); await projects.assertProjectOwner(projectId, token); const body = approvalSchema.parse(await request.json()); const sample = await new PostgresNarrationRepository(database, factualityAuditDeployment(env).contract).approveSample(projectId, body.sampleId); return NextResponse.json({id: sample.id, approved: Boolean(sample.approvedAt)}); } catch (error) { const safe = safeNarrationRouteError(error); return NextResponse.json({error: safe.code}, {status: safe.status}); } }
